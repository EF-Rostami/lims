import { CustomerDashboard } from "@/features/saas/customers/components/CustomerDashboard";

export const metadata = {
  title: "Customers | BLIMS Platform",
};

export default function CustomersPage() {
  return <CustomerDashboard />;
}
