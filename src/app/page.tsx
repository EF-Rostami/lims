// eslint-disable-next-line @typescript-eslint/no-unused-vars
import { redirect } from "next/navigation";

export default function RootPage() {
  // redirect("/dashboard");
  return(
    <h1> Salam- root page</h1>
  )
}